# API Setup

Create local API credentials in `.env` when future phases need them.

Expected variables:

```env
RIME_API_KEY=
DEEPGRAM_API_KEY=
OPENAI_API_KEY=
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
```

Do not commit `.env`. Phase 1 does not call any external APIs.
