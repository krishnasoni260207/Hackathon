# Technology

Phase 1 uses:

- Python for application code.
- Streamlit for the local UI.
- python-dotenv for future local environment loading.
- HTTP and realtime client libraries for later service integrations.
- pytest for tests.

Future phases are expected to connect Speech-to-Text, an LLM, Rime TTS, and LiveKit. Those integrations are not active in Phase 1.
